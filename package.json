{
  "name": "cggenerator",
  "version": "1.0.0",
  "description": "Tech Certificate Generator",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "concurrently \"cd backend && python main.py\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}