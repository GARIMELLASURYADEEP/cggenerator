# Frontend deployment configuration for Render
# This file tells Render how to build and run your React frontend

# Build command
npm install && npm run build

# Publish directory (where the built files are)
build

